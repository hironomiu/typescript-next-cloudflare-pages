// TODO: 'Sample.test.tsx' cannot be compiled under '--isolatedModules' because it is considered a global script file.
describe('first', () => {
  it('test', () => {
    expect(1 + 1).toBe(2)
  })
})
